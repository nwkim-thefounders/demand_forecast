# Streamlit 애플리케이션 개발을 위한 AI 에이전트 지침서

## 1. 파일 명칭 규칙 (File Naming Conventions)
- **일반 페이지:** 각 `.py` 파일은 하나의 페이지를 구성하며, 파일명은 반드시 `app_'숫자2자리'_페이지목적.py` 형식으로 작성해야 합니다.
  - *예시: `app_01_dashboard.py`, `app_02_data_analysis.py`*
- **특수 목적 파일:** 아래의 파일들은 예외 케이스로 지정되며, 정해진 명칭을 그대로 사용해야 합니다.
  - `app_00_main.py` (애플리케이션의 메인 엔트리 포인트)
  - `snowflake_SQL.py` (SQL 쿼리 관리 및 실행)
  - `app_cache_load.py` (데이터 캐싱 처리 전용)

## 2. 메인 엔트리 및 페이지 연결 (Main Entry & Navigation)
- 애플리케이션의 시작점은 `app_00_main.py` 파일입니다.
- 사용자가 다른 페이지로 이동할 수 있는 내비게이션 및 페이지 라우팅 구조는 반드시 `app_00_main.py` 내부에서 정의하고 연결해야 합니다.

## 3. 페이지 내부 구조 및 함수 분리 (Page Structure & Clean Code)
- **UI 렌더링 함수:** 각 페이지의 전체적인 레이아웃과 구조는 `show_페이지명()` 함수 내에서 한눈에 파악할 수 있도록 작성해야 합니다.
  - `show_페이지명()` 함수 내부에는 시각적 요소 및 UI 구성 요소만 배치합니다. (예: `st.write()`, `st.button()`, `st.selectbox()` 등)
- **비즈니스 로직 함수:** UI 렌더링을 제외한 데이터 처리, 연산, API 호출 등의 모든 작업은 별도의 함수로 분리해야 합니다.
  - 분리된 내부 연산 함수는 언더바(_)로 시작하는 `_함수명()` 형식의 private 네이밍 규칙을 따릅니다.

## 4. 캐싱 전략 (Caching Strategy)
- 본 애플리케이션은 Streamlit Cloud에 배포되므로 효율적인 자원 관리가 필수적입니다.
- 캐시(Cache) 등록이 필요한 대용량 데이터나 무거운 연산의 경우, 시스템이 무조건 캐싱을 수행하게 해서는 안 됩니다.
- 사용자가 캐시 등록 여부를 UI에서 직접 선택할 수 있도록 제어 옵션(예: 토글, 체크박스 등)을 반드시 제공해야 합니다.